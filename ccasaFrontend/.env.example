# -----------------------------------------------------------------------------
# ccasa backend (Spring Boot, JWT)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_API_BASE_URL=http://localhost:8082
# Alineado con server.port en ccasaBackend/application.properties
# Si usas perfil local (application-local): http://localhost:8081

# -----------------------------------------------------------------------------
# App
# -----------------------------------------------------------------------------
BASEPATH=
NEXT_PUBLIC_APP_URL=http://localhost:3000${BASEPATH}

# Opcionales (heredados del template Materio). Déjalos vacíos si no usas enlaces externos.
NEXT_PUBLIC_DOCS_URL=
NEXT_PUBLIC_PRO_URL=
NEXT_PUBLIC_REPO_NAME=ccasa
